enum TaskStatus { all, pending, active, completed }

enum AuthenticationStatus {
  authenticated,
  authenticationFaild,
  unAuthenticated,
  userExists,
  connectionError,
  completeTheData,
  userVerification,
}

enum RegistrationStatus { registering, none }

enum NetworkStatus { connected, disconnected }

enum TaskOperationStatus { success, faild, none }
