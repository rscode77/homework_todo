enum TaskStatus { all, pending, active, completed }

enum LoginErrors { userNotFound, noData }

enum RegistrationStatus { registering, none, registered }

enum NetworkStatus { connected, disconnected }
